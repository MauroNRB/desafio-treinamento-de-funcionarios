{% macro create(url) %}
<br>
<div class="form-actions form-row text-center">
    <button type="submit" class="btn btn-primary btn-lg"><i class="glyphicon glyphicon-ok"></i> {{ 'Criar'|trans }}</button>
    <a href="{{ app.request.headers.get('referer', url) }}" class="btn btn-danger btn-lg"><i class="glyphicon glyphicon-remove"></i> {{ 'Cancelar'|trans }}</a>
</div>
{% endmacro %}

{% macro update(url) %}
<br>
<div class="form-actions form-row text-center">
    <button type="submit" class="btn btn-primary btn-lg"><i class="glyphicon glyphicon-save"></i> {{ 'Salvar Alterações'|trans }}</button>
    <a href="{{ app.request.headers.get('referer', url) }}" class="btn btn-danger btn-lg"><i class="glyphicon glyphicon-remove"></i> {{ 'Cancelar'|trans }}</a>
</div>
{% endmacro %}
